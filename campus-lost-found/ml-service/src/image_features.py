def extract_features(image_path: str) -> list[float]:
    """
    Placeholder for an image feature extraction pipeline (e.g. CLIP).
    For now, returns a dummy feature vector to avoid adding heavy 
    vision dependencies like torch and torchvision by default.
    """
    return [0.0] * 512
